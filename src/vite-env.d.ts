/// <reference types="vite/client" />

/** The commit this bundle was built from, injected by `vite.config.ts`.
 *  It is printed in every PDF's end matter so a reader can tell which code
 *  produced the file in their hand without anyone grepping a served bundle. */
declare const __BUILD_ID__: string;
